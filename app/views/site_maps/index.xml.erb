<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
   <url>
      <loc><%= @host %></loc>
   </url>
   <url>
      <loc><%= "#{@host}#{statics_path}" %></loc>
   </url>
  
   <% @tutos.each do |tuto| %>
     <url>
        <loc><%= "#{@host}/tutos/#{tuto.slug}" %></loc>
     </url>
   <% end %>
</urlset>