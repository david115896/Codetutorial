<%= form_with(model: tuto, local: true) do |form| %>
  <% if tuto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tuto.errors.count, "error") %> prohibited this tuto from being saved:</h2>

      <ul>
      <% tuto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
